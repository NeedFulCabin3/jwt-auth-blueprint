# jwt-auth-blueprint
A stateless authentication starter kit built with FastAPI, PyJWT, and bcrypt password hashing. It includes isolated endpoints for user registration, bearer token generation, and secure route protection via dependency injection. Use it as a solid foundation or reference baseline for building robust, scalable Python web APIs.
